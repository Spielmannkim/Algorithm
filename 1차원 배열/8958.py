# OX퀴즈

T = int(input()) # 테스트 개수
arr = [] # 빈 배열
for _ in range(T): # 테스트 개수만큼 반복
    OX = input() # OX를 문자열로 입력받음
    arr.append(OX) # 빈배열 arr에 입력받은 문자열 OX를 추가
    # 여기서 부터 모르겠어서 구글링 후 8958_1.py에 코드 주석만 내가 직접 달음.
